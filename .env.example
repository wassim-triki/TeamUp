# SECURITY WARNING: keep the secret key used in production secret!
# Generate a new key with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here-change-this-in-production

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# Database Configuration
# For SQLite (default for local development), leave DB_ENGINE empty or commented out
# For MySQL:
# DB_ENGINE=django.db.backends.mysql
# DB_NAME=teamup_db
# DB_USER=root
# DB_PASSWORD=
# DB_HOST=localhost
# DB_PORT=3306

# For PostgreSQL (local development):
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=teamup_db
# DB_USER=postgres
# DB_PASSWORD=your_password
# DB_HOST=localhost
# DB_PORT=5432

# For Production on Render (automatically set by Render):
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=wsmtriki@gmail.com
EMAIL_HOST_PASSWORD=google_app_password
DEFAULT_FROM_EMAIL="TeamUp <noreply@teamup.com>"
GEMINI_API_KEY=example-key-not-real
MYOPENAI_API_KEY=example-key-not-real


